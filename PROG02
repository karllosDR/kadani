#include <stdio.h>
#include <string.h>
void main() {
    char str[10];
    printf("Digite uma string de no maximo  10 digitos : ");
    printf("Digite uma palavra de até 10 digitos : ");
    scanf("%[^\n]%*c", &str);
    printf("A string possui %d letras\n", strlen(str));
}
